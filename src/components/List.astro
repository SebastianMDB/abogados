---
interface Props {
    contentList: string[];
    className?: string;
}
---

<ul class={`${Astro.props.className}`}>
  {
    Astro.props.contentList.map((content) => (
      <li class="">{content}</li>
    ))
  }
</ul>